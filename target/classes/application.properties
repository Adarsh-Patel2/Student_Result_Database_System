spring.application.name=SRMS

# MySQL connection
spring.datasource.url=jdbc:mysql://localhost:3306/srms_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Run schema.sql on every startup (safe: uses CREATE TABLE IF NOT EXISTS)
spring.sql.init.mode=always

server.port=8080
